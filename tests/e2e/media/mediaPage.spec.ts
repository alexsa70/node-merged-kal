import { test } from '@playwright/test';

test.describe.skip('E2E media page', () => {
  test('source pytest file is empty (no scenarios to port)', async () => {
    // Intentionally empty: test_media_page.py has no tests.
  });
});
