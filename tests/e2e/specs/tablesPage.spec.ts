import { test } from '@playwright/test';

test.describe.skip('E2E tables page', () => {
  test('tables page was removed; functionality moved to Documents', async () => {
    // Mirrors source pytest suite where this entire class is skipped.
  });
});
