import { test } from '@playwright/test';

test.describe.skip('E2E projects page', () => {
  test('source pytest file is empty (no scenarios to port)', async () => {
    // Intentionally empty: test_projects_page.py has no tests.
  });
});
